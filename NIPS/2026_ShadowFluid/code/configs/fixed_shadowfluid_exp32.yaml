inherits: base_exp32.yaml

output_dir: ../results/bench_exp32/fixed_shadowfluid

model:
  name: fixed_shadowfluid
  latent_dim: 6
  reference_mode: coupling_greedy
  reference_budget: 6
  R_hops: 1
  pool_hops: 2
  pool_max_candidates: 12

train:
  selection_metric: loss_total

loss:
  w_leakage: 0.0
