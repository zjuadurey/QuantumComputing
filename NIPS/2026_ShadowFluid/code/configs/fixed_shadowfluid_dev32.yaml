inherits: base_dev32.yaml

output_dir: ../results/bench_dev32/fixed_shadowfluid

model:
  name: fixed_shadowfluid
  latent_dim: 8
  reference_mode: coupling_greedy
  reference_budget: 8
  R_hops: 1
  pool_hops: 2
  pool_max_candidates: 16

train:
  selection_metric: loss_total
